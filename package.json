{
  "name": "patchr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha --reporter spec --recursive --bail --check-leaks test/",
    "cover": "nyc --reporter lcov ./node_modules/mocha/bin/_mocha --reporter spec --recursive --bail --check-leaks test/",
    "clean": "rm -fr .nyc_output && rm -fr coverage",
    "build": "node-gyp rebuild"
  },
  "author": "Andrew Johnston",
  "license": "ISC",
  "gypfile": true,
  "dependencies": {
    "nan": "2.7.0"
  },
  "devDependencies": {
    "mocha": "4.0.1",
    "nyc": "11.2.1",
    "should": "13.1.2"
  }
}
